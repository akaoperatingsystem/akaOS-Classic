# akaOS Classic GRUB Configuration
set timeout=0
set default=0

# Load video driver (required for UEFI framebuffer)
insmod all_video

menuentry "akaOS Classic" {
    multiboot2 /boot/akaos.bin
    boot
}
